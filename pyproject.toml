[project]
name = "frigateconnect"
version = "0.1.0"
description = "Export Frigate alert clips as universally compatible MP4 videos"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27,<1",
]
